spring:
  datasource:
    url: jdbc:mysql://localhost:3306/easybud?useSSL=true&useUnicode=true&serverTimezone=Asia/Seoul
    username: admin
    password: 12345678
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100
        dialect: org.hibernate.dialect.MySQLDialect